"use strict";

const num = 1;
const num1 = 2;

const sum = (a, b) => a + b;

const multiply = (a, b) => a * b;

const minus = (a, b) => a - b;

console.log(minus(num, num1));
console.log(sum(num, num1));
console.log(multiply(num, num1));
